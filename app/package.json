{
  "name": "look-ma-no-secrets",
  "version": "1.0.0",
  "description": "Bella Baxter demo — zero secrets in Docker Compose",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "echo 'No build step needed'"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@bella-baxter/sdk": "0.1.1-preview.78",
    "express": "^5.2.1",
    "pg": "^8.13.0"
  }
}
